#include "main.h"

/**
 *_isdigit - checks if the input is a number
 *@c:input value to be checked
 *
 * Return: 0
 */

int _isdigit(int c)
{

	if (c >= 48 && c <= 57)
	{
		return (1);
	}
	else
	{
		return (0);
	}
	return (0);
}
